# Известные ограничения среды и воркэраунды

Цель файла — не передиагностировать одно и то же в новой сессии. Если
нашли новый workaround среды/инструмента — дописывайте сюда в конце
сессии, не только в память агента (память не переживает переход на
другой инструмент/аккаунт, этот файл — переживает).

## ACF `pillar_card_groups` vs `post_content` — гочтя знову наступив на ту саму граблю, 2026-09-06

Той самий баг, що вже задокументовано 2026-09-05 у коментарі `inc/acf-
admin-ux.php` ("post_content on these still holds the old hand-authored
HTML as a non-destructive fallback, but it's not what an editor should
touch") — і всупереч тому, що це вже було відомо, наступний раунд
видалення сторінок (2026-09-06) знову відредагував `post_content`
pillar-сторінки (122) замість реального джерела, і тільки візуальна
перевірка в браузері (не сам факт "wp post update — Success") показала,
що картки, які мали зникнути, все ще рендеряться.
**Як перевіряти на майбутнє, щоб не наступати знову**: перед тим, як
редагувати вміст будь-якої pillar/rich-service сторінки (ID є в
`koval_legal_pillar_services()` або `koval_legal_rich_services()`,
`inc/service-lists.php`) — спершу `wp eval 'echo
json_encode(get_field("pillar_card_groups", ID));'`, і якщо там є дані —
редагувати САМЕ це поле (`update_field()`), `post_content` для цих ID
чіпати взагалі не варто, він мертвий. Після будь-якої такої правки —
`curl` живої сторінки й `grep` на видалений slug, а не тільки перевірка
БД-поля, яке ти щойно змінив (те, що поле оновилось, не доводить, що
воно й рендериться).

## Code-level контраст pre-аудит (полезный метод при отключённом Chrome), 2026-09-06

Когда Claude in Chrome недоступен, а откладывать контраст-аудит целиком
до следующей сессии не хочется — рабочий частичный метод: вытащить все
цветовые CSS-токены (`grep -n -- "--[a-z-]*:" style.css`), затем python-скриптом
посчитать WCAG-контраст (формула из спецификации, luminance +
`(L1+0.05)/(L2+0.05)`) для всех пар токенов, которые реально
используются вместе как текст+фон в CSS (найти это через `grep`
`color:var(--x)` рядом с известным `background`/`background-color`
контекстом selector'a). Это ловит **тот же класс бага**, что уже был
найден и исправлен один раз (`--gold` = 2.8:1 как текст на светлом
фоне) — в этот раз нашёл ещё 2 отдельных использования того же токена
(`.stepper-card`/`.svc-body .process-step` arrow-иконки), пропущенных в
прошлый раз, потому что тот аудит не был системным по всем
использованиям токена.
**Ограничение метода**: это НЕ замена браузерного аудита. Не ловит:
контраст в контенте, вставленном через ACF/WYSIWYG (произвольные
inline-цвета вне системы токенов), сгенерированные JS классы,
реальный rendered-контраст с учётом наложения полупрозрачных фонов
(`rgba(...)` слоёв друг на друга), псевдоэлементы с фоном не из токена
(изображения/градиенты). Использовать как быстрый code-level pass
**в дополнение** к browser-аудиту, не вместо него — статус находок
всегда остаётся [код], пока не подтверждено визуально.

## wget-экспорт для демо (Cloudflare Pages) — пятая готча, найдено 2026-09-06

**5. `<picture><source type="image/webp" srcset="https://law-firm.ddev.site/...">`
не переписывается wget в локальный/относительный путь вообще** — только
`<img src>` внутри того же `<picture>` конвертируется корректно. Причина
та же, что у готчи №1 ниже (wget физически не скачивает и не обрабатывает
`<source srcset>`), но проявление другое: там речь была об отсутствующих
файлах, здесь — о невырезанном абсолютном dev-домене в ссылке на файл,
который на самом деле уже лежит в экспорте (после фикса №1, копирования
`uploads` напрямую). Chrome/большинство браузеров поддерживают WebP →
`<picture>` всегда берёт `<source>`, а не запасной `<img>`, и раз
`law-firm.ddev.site` не резолвится публично — картинка не рендерится
вообще, при полностью корректном HTML/статус-коде у самого `<img>`.
**Пойман только визуально** (пустое место на `/pro-nas/` вместо фото
основателя) — curl/`file`-байт-проверка (см. готчу №2 ниже) её не ловит,
ссылка синтаксически валидна и указывает на реальный существующий файл,
просто на недоступном извне хосте.
**Фикс**: `perl -pi -e 's{srcset="https://law-firm\.ddev\.site(/[^"]*)"}{srcset="$1"}g'`
по всем `.html`-файлам экспорта — вырезать домен, оставить только путь
(тот же принцип, что и для shortlink/`?ver=`-фиксов выше — абсолютный
путь без домена, не относительный с вычислением глубины).
**Как проверять на будущее**: после любой пересборки экспорта — открыть
именно `/pro-nas/` (там единственное на сайте `<picture>` без запасного
контента вокруг) в реальном Chrome и визуально подтвердить, что фото
рендерится, не полагаться на то, что фикс №1 (копирование uploads) решает
и эту проблему тоже — это два разных бага одного класса.

## wget-экспорт для демо — шестая готча: баг у власному fix_export.py, знайдено й виправлено в джерелі 2026-09-06

**6. Регекс для `?ver=`-фіксу (готча №2 нижче) чіпляв тільки хвіст
`style.css@ver=\d+`/`main.js@ver=\d+`, без префіксу
`wp-content/themes/koval-legal/...` перед ним.** Коли повний рядок був
`../../wp-content/themes/koval-legal/style.css@ver=123`, regex матчив
лише `style.css@ver=123` (підрядком, не з початку), і заміна на
абсолютний шлях `/wp-content/themes/koval-legal/style.css` **не
видаляла** старий префікс — результат: подвоєний шлях
(`.../koval-legal/assets/js//wp-content/themes/koval-legal/assets/js/main.js`),
який 404-ить. Це вже траплялося в попередній сесії (2026-09-05) —
тоді пропатчили тільки ВИВІД (готовий export) вручну, не сам скрипт,
тому баг гарантовано повторився на наступному запуску з нуля.
**Фікс у джерелі**: `ver_re`/`ver_js_re` тепер явно включають
опціональний префікс `(?:wp-content/themes/koval-legal/(?:assets/js/)?)?`
перед іменем файлу, щоб префікс теж потрапляв у матч і видалявся разом
з рештою. **Урок**: патчити тільки готовий вивід одноразового скрипту —
це відкладена бомба; коли баг знайдено в такому скрипті, фіксити треба
джерело скрипту негайно, навіть якщо поточний прогін вже "врятовано"
ручним патчем — інакше кожен наступний rebuild-with-fresh-wget
відтворює той самий баг з нуля. Перевірено: чистий прогін з нуля після
фіксу дав 0 подвоєних шляхів.

## wget-экспорт для демо (Cloudflare Pages) — четыре готчи, найдено 2026-09-05

**4. Найдено пользователем при верификации после деплоя, не мной —
все 41 карточки каталога `/poslugy/` вели на битые shortlink-URL
(`../index.html%3Fp=100.html` вместо `/poslugy/dublikat-svidotstva-dratss/`),
несмотря на то, что на живом ddev-сайте эти же карточки рендерят
правильный `<a href="https://law-firm.ddev.site/poslugy/.../">`.**
Причина: WordPress вставляет свой родной `?p=ID` shortlink
(`<link rel="shortlink" href="?p=100">` в `<head>`, плюс WP может подставить
его же в контекстах, где абсолютный canonical URL ещё не резолвился на
момент рендера конкретного блока) — `wget --convert-links` добросовестно
конвертирует этот shortlink в файл наравне с настоящим постоянным URL,
но карта "ID → реальный слаг" при этом не строится, потому что
`--convert-links` работает по ссылкам, а не по контенту. Итог: 1797
вхождений в 64 файлах экспорта, вообще не пойманных предыдущими тремя
проверками (все они проверяли CSS/JS/картинки, ни одна не прогоняла
carefully все карточки каталога на предмет реального href).
**Это тот самый класс проблемы**, что и задокументированный в
`project_koval_demo_deployment` "WordPress `<link rel="shortlink"
href="?p=ID">` tags getting mis-associated with real nav links" —
известная и раньше, но **в этот раз не была явно перепроверена** до
пользовательского запроса на точное сравнение чисел.
**Фикс**: `wp post list --post_type=service,page,post,case_study
--post_status=publish --fields=ID,url --format=csv` даёт полную карту
ID→постоянный URL; затем один Python-проход по всем `.html` файлам
экспорта заменяет `(?:\.\./)*index\.html%3Fp=(\d+)\.html` на путь из
карты (убрав домен, оставив только path).
**Как проверять на будущее**: сравнение "число услуг в БД" vs "число
уникальных `href` в карточках каталога на деплое" — простой и надёжный
дифф-чек, который сразу ловит этот класс бага; включить в стандартную
пост-деплойную проверку, не только curl статус-кодов и скриншот.

## wget-экспорт для демо (Cloudflare Pages) — первые три готчи, найдено 2026-09-05

**1. `wget --mirror --page-requisites` не скачивает `<picture><source
srcset="...">` вообще** — только обычные `<img src>`. Весь сайт доставляет
картинки через `<picture><source type="image/webp"><img></picture>` (см.
`inc/webp-images.php`), поэтому **все .webp-файлы молча выпадали из
экспорта** — 0 из 18. `<img>`-фолбек иногда докачивался, иногда нет.
Итог: фото могло не отображаться в браузере, хотя curl на `/wp-content/...`
давал 200 (Cloudflare Pages отдаёт `index.html` для любого несуществующего
пути с кодом 200 — тот самый "false 200", уже задокументированный раньше;
без реальной проверки байтов через `file` эта подмена незаметна).
**Рабочий фикс**: не полагаться на wget для `wp-content/uploads` вообще —
после `--mirror` скопировать реальную папку `uploads` напрямую с хоста
(`cp -r wp-content/uploads/* <export>/wp-content/uploads/`), она и так
лежит на файловой системе рядом с проектом, не только в ddev-контейнере.

**2. Побочная находка, не только про экспорт**: `oleg-koval-682x1024.jpg`
(fallback для фото засновника) **не существует даже на самому dev-сайті**
(404 напряму, перевірено curl) — сторінка тримається виключно на
WebP-джерелі. Якщо колись зникне WebP-підтримка в браузері відвідувача
(або сам webp-файл), фолбек не спрацює. Реальний баг сайту, не тільки
експорту — вартий окремого фікса (перегенерувати .jpg з .webp або
перезалити оригінал), не зроблено в рамках цієї сесії.

**3. `?ver=123` у файлах CSS/JS ламає деплой на Cloudflare Pages двічі
підряд, різними способами:**
- Перше очевидне: `--adjust-extension` перетворює `style.css?ver=X` на
  файл із **буквальним** символом `?` в імені (`style.css?ver=X.css`), а
  `--convert-links` водночас переписує HTML-посилання на
  **URL-екранований** варіант (`style.css%3Fver=X.css`) — імена не
  збігаються навіть якщо перейменувати файл на `%3F` буквально: Cloudflare
  Pages не віддає такий файл як статику (503 або SPA-fallback 200,
  замість реального файлу — перевірено обома способами).
  **Фікс**: не намагатися зберегти версійність у імені файлу — перейменувати
  файл на "чисте" ім'я (`style.css`, без `?ver=`) і тим самим regex'ом
  прибрати `%3Fver=\d+(\.css|\.js)?` з усіх HTML-посилань. Для статичного
  експорту версійність все одно не потрібна (немає build-системи, що б її
  використовувала).
- **Друге, підступніше**: після виправлення #1 наївний
  `sed 's|\.\./wp-content/|/wp-content/|g'` ламає посилання на глибоко
  вкладених сторінках (`/poslugy/slug/`, 2 рівні вкладеності), бо там
  оригінальний відносний шлях був `../../wp-content/`, а не `../wp-content/`
  — заміна одного рівня "../" залишає висячий залишок (`..//wp-content/`),
  який теж не резолвиться. **Правильний фікс**: regex, що прибирає **всі**
  повторення `(\.\./)+` перед `wp-content/`/`wp-includes/`, а не рівно одне
  входження — `perl -pi -e 's{(\.\./)+/?(wp-content/|wp-includes/)}{/$2}g'`.
  Урок: після будь-якого blanket-фіксу відносних шляхів обов'язково
  перевіряти сторінку саме на найглибшому рівні вкладеності сайту (тут —
  `/poslugy/<slug>/`), не тільки головну — баг на глибині 2 не проявляється
  на глибині 0/1.

## curl/grep подтверждают HTTP-уровень, не вёрстку

**Правило**: curl/grep/wget подтверждают только то, что происходит на
уровне HTTP-транспорта и сырой разметки — статус-коды, заголовки,
наличие определённого текста/тега в HTML. Они **не показывают**, как
страница реально рендерится: раскладку, видимость блока, применённые
CSS-правила, правильно ли резолвится путь к ассету в браузере (в
отличие от того, что он просто существует на диске/отдаёт 200 сам по
себе).

**Почему это зафиксировано**: 2026-09-03 curl/grep-проверки (HTML-текст +
HTTP-статусы) дважды подряд пропустили реальные визуальные баги:
1. Схлопнувшийся `.svc-grid` — секция была полностью невидима на
   странице, при этом весь нужный текст/разметка присутствовали в HTML
   и отдавались с 200 (проблема была чисто в CSS-раскладке).
2. Сломанный путь к CSS/JS-ассетам — раздевал стилизацию со всего live-сайта,
   при этом сами файлы могли отдавать корректный статус по отдельности
   (проблема была в том, как браузер их резолвил/подключал в контексте
   страницы, не в самом файле).

Второй баг поймал сам пользователь по скриншоту с телефона — это должно
было быть поймано агентом раньше пользователя.

**Как применять**: любое изменение, которое может повлиять на
CSS/раскладку/видимость/подключение ассетов — WYSIWYG-контент, стили,
пути к файлам, деплой — обязательно проверять вживую в браузере (Chrome
+ скриншот на нескольких ширинах), не только curl/grep. curl/grep
достаточны только для проверки чисто транспортных вещей: статус-коды,
заголовки, наличие/отсутствие текста в сыром HTML/JSON-ответе (см.
пример: XML-RPC 403, REST `/users` 404, Schema.org JSON-LD — эти
проверки по природе транспортные/данные, там curl достаточен). Это уже
закреплено как обязательное правило в CLAUDE.md ("Frontend verification") —
этот пункт здесь просто фиксирует историю/причину, почему правило
появилось, для контекста.

## Реальная keyboard-Tab-навигация через Claude in Chrome не работает в этой песочнице, найдено 2026-09-06

Пытался проверить tab-order/focus-indicator настоящими нажатиями `Tab`
(через `computer:key`, не программный `.focus()` — см. ниже, почему
`.focus()` тоже не годится). Симптом: `document.activeElement` после
`Tab` не двигается с `BODY`, хотя мышиные клики и `javascript_exec` на
той же вкладке работают штатно (навигация по кликам, чтение DOM — всё
ок). Диагностика: `document.visibilityState` и `document.hidden` на
управляемой расширением вкладке — **всегда `'hidden'`/`true`**, даже
у только что созданной через `tabs_create_mcp` пустой вкладки. Т.е.
вкладки, которыми управляет автоматизация, никогда не являются реально
видимой (foreground) вкладкой Chrome с точки зрения самого браузера —
а нативная browser-level Tab-focus-навигация (в отличие от кликов и
JS-исполнения, которые CDP успешно доставляет в любую вкладку
независимо от видимости) требует foreground-статуса вкладки. Пробовал
`wmctrl -a`/`-i -a` активировать окно на уровне X11 — не помогло
(окно и так было OS-активным, проблема не в этом).
**Родственная, но отдельная проблема**: программный `el.focus()` через
JS тоже не годится как замена — двигает `document.activeElement`
корректно, но не обязательно триггерит `:focus-visible` (эвристика
Chrome по "недавний ввод был с клавиатуры"), поэтому даёт ложные
"NONE" для элементов, полагающихся на нативный default outline браузера
(который специфичен именно для `:focus-visible`, не для `:focus`).
**Рабочая замена, когда столкнётесь с этим снова**: код-уровневая
проверка вместо реального keyboard walk — `grep -n "outline:\s*none\|outline:\s*0"`
по всему CSS темы, и для каждого найденного места убедиться, что рядом
есть видимая замена (`border-color`/`box-shadow` и т.п.). Плюс `grep
":focus"` — посмотреть, что вообще стилизуется явно (то, что НЕ
стилизуется явно и не убирает outline, полагается на браузерный
дефолт — он WCAG-совместим by construction, пока ничего его не убрало).
Плюс проверить `main.js`/тему на `tabindex`/`.focus()` вручную (grep),
не через реальную симуляцию. Статус такой проверки — всегда **[код]**,
не [browser], даже если она даёт правильный ответ — честно фиксировать
это ограничение, не выдавать код-уровневый эквивалент за настоящий
keyboard-audit.

## Viewport-тестирование в этой песочнице (Linux, MATE desktop, Claude in Chrome)

- **DevTools Device Toolbar (`Ctrl+Shift+M`) не активируется через
  CDP-автоматизацию.** CDP инжектит keyboard events в контекст страницы
  (Input domain на конкретный tab), а переключение Device Toolbar — это
  shortcut уровня браузерного chrome (обрабатывается процессом самого
  Chrome, не рендерером страницы) — инъекция до него физически не долетает.
  Не тратить время на повторные попытки другими комбинациями клавиш — это
  архитектурное ограничение, не баг конкретной комбинации.
- **`resize_window` (MCP-тул) синхронизируется с реальным viewport вкладки
  только ОДИН раз — в момент создания вкладки.** Дальнейшие вызовы
  `resize_window` на той же вкладке репортят успех, но `window.innerWidth`
  не меняется. Подтверждено: реальное X11-окно (проверено через
  `wmctrl -lG`/`xwininfo`) резайзится корректно каждый раз, а CDP-рендер
  вкладки — нет.
  **Рабочий метод**: резайзить окно **ДО** создания вкладки (через
  `resize_window` на только что созданной пустой вкладке, или напрямую
  через `wmctrl -r "Google Chrome" -e 0,x,y,width,height` из Bash), потом
  создавать/переиспользовать вкладку и сразу проверять
  `window.innerWidth` через `javascript_tool` — не доверять только тексту
  "Successfully resized".
- **Физический потолок сужения окна в этой песочнице — ~520px.** Chrome/WM
  не даёт окну (и его реальному content area) стать уже, даже если X11-окно
  формально резайзится до меньшего значения (проверено — `xwininfo`
  показывал 500px, а `window.innerWidth` оставался на предыдущем
  синхронизированном значении). Настоящих 375px в этой конкретной среде не
  добиться.
- **Для темы koval-legal это не проблема**: брейкпоинты в CSS —
  `max-width: 640px / 680px / 768px / 960px` (проверено `grep @media` по
  всем CSS-файлам темы). Между 375 и 640px новых правил нет — визуальный
  тест на 520px функционально эквивалентен тесту на 375px **именно для
  этой темы**. Если брейкпоинты в теме изменятся, эту эквивалентность нужно
  перепроверить, не считать постоянным фактом.
- **Первый вызов `computer:screenshot` после навигации/клика/resize часто
  таймаутит** (`CDP sendCommand "Page.captureScreenshot" timed out after
  30000ms`). Немедленный повтор того же вызова обычно успешен. Не считать
  это реальным сбоем страницы с первого раза — просто повторить один раз.

## ACF Options Page — конвенция именования (наступал дважды за сессию)

Поле, объявленное на Options Page (`inc/acf-fields.php`, options page slug
`koval-site-settings`) с именем поля `gtm_id`, хранится в `wp_options`
**не** под ключом `gtm_id`, а под **`options_gtm_id`** — плюс служебная
строка `_options_gtm_id`, хранящая ключ поля ACF (`field_koval_gtm_id`)
для внутреннего резолвинга.

- Поле GTM ID → `options_gtm_id`
- Поле Meta Pixel ID → `options_meta_pixel_id`
- То же правило для любого другого поля этой Options Page (например,
  `category_mini_cta`/`category_show_on_homepage` — это term meta, не
  option, у них своя история, но тот же принцип "имя поля ≠ имя строки
  хранения" применим).

**Правило на будущее**: `get_field('x', 'option')` / `update_field('x',
$v, 'option')` в коде — всегда правильно, они уже резолвят этот префикс
сами. Но при ручной проверке/дебаге напрямую через `wp option get x` /
`wp option update x ...` **обязательно добавлять префикс `options_`** —
иначе `get` вернёт "not found", а `update` молча создаст несвязанную
мёртвую опцию, что выглядит как "фича не работает", хотя на самом деле
просто неверное имя опции.

## Rank Math — sitemap-система и её кэш

- **`wp_sitemaps_post_types` (фильтр WP-ядра) не действует, пока активен
  Rank Math.** Rank Math полностью заменяет sitemap-систему ядра своей
  собственной — фильтры ядра для неё не значат ничего. Реальный контроль:
  опция `rank-math-options-sitemap`, ключ `pt_{post_type}_sitemap` =
  `on`/`off` (UI: Titles & Meta → Sitemap Settings; CLI:
  `wp option patch update rank-math-options-sitemap pt_case_study_sitemap off`).
- **Сгенерированный XML кэшируется отдельно** (опция
  `rank_math_sitemap_cache_files` + реальные файлы). Смена настройки
  через wp-cli/прямую запись в БД **не** триггерит автоматическую
  инвалидацию (в отличие от смены через UI, которая проходит через
  сохранение настроек и, вероятно, чистит кэш сама — не проверялось).
  После любой программной смены `pt_*_sitemap` нужно явно:
  ```
  wp eval 'RankMath\Sitemap\Cache::invalidate_storage();'
  ```
  Без этого `sitemap_index.xml` продолжит отдавать старый список.
- **Ранее решённая, но похожая по духу готча**: Rank Math может стоять
  `active` в `wp plugin list` и не выдавать ни одной ошибки, будучи при
  этом 100% неактивным на фронтенде (никакого title/meta/sitemap) — если
  `Registration::invalid` истинно (незавершённый setup wizard). Не нужен
  браузер/wizard для фикса — `wp option update rank_math_registration_skip 1`
  (официальный эквивалент кнопки "Skip"). См. `migration-status.md`.

## XML-RPC — стандартный фильтр не работает как ожидается

`add_filter('xmlrpc_enabled', '__return_false')` — учебниковый способ
отключить XML-RPC — **не блокирует** реальные запросы к `xmlrpc.php`.
Проверено напрямую: `demo.sayHello` через POST всё равно проходил с
одним этим фильтром. Он гейтит только pingback-специфичные методы
внутри, не сам endpoint. Рабочий фикс — прямой 403 на `init` для любого
запроса к `xmlrpc.php` (см. `inc/security-hardening.php`). Если где-то
всплывёт похожий "стандартный туториальный фикс не работает" — сначала
тестировать реальным запросом, не доверять тому, что фильтр называется
правильно.

## Фейковые `.webp`-файлы (наследие инцидента 2026-09-02)

Как минимум один файл (`oleg-koval-682x1024.webp`) существовал с
расширением `.webp` и корректным `Content-Type: image/webp` в заголовке
ответа, но его реальные байты были HTML-страницей (fallback/404 от
разбора после восстановления БД) — это прошло все поверхностные проверки
(200 статус, правильный content-type) и было поймано только через `file
<path>`, показавший "HTML document" вместо image data.

**Правило на будущее**: если картинка не рендерится именно при вставке
через `<picture><source type="image/webp">`, но URL/статус проверяются
нормально по отдельности — проверять реальные байты файла (`file`
локально, или скачать и проверить магические байты), не доверять
статус-коду и content-type как доказательству, что это настоящее
изображение — особенно после любого восстановления из бэкапа/инцидента.

## ddev nginx — заголовки кэша статики, специфика dev-окружения

`.ddev/nginx_full/nginx-site.conf` явно прописывает:
```
location ~* \.(js|css)$ { expires -1; }
location ~* \.(jpg|jpeg|gif|png|...|webp|...)$ { expires max; }
```
т.е. в **этом dev-окружении** `.css`/`.js` намеренно отдаются без кэша
(`Cache-Control: no-cache`), а картинки — с 10-летним кэшем. Шрифты
(`.woff2`) не попадают ни в одно из этих правил вообще — без явных
кэш-заголовков. Это **не баг темы** и не нужно чинить в PHP/теме — это
конвенция ddev для удобства разработки. Не факт, что на реальном
Apache/LiteSpeed-хостинге будет так же (может быть лучше или хуже) — не
переносить эту особенность как "нормальное поведение" при аудите прод-хостинга,
перепроверять с нуля, когда появится доступ к реальному серверу.

## SQL query profiling — нет `wp profile` (пакет не установлен)

`wp profile stage` (пакет `wp-cli/profile-command`) не установлен и не
ставился (не хотелось тянуть лишнюю зависимость ради разового замера).
Рабочая альтернатива для разового замера числа/времени SQL-запросов на
страницу: временно
1. `define('SAVEQUERIES', true)` в `wp-config.php`,
2. mu-plugin с хуком на `shutdown`, логирующий `$wpdb->num_queries` /
   `$wpdb->queries` в `error_log`,
3. curl нужных страниц,
4. прочитать `wp-content/debug.log`,
5. **обязательно откатить оба временных изменения** (define + mu-plugin)
   после замера — это не постоянная конфигурация.

## Разное

- **`sg docker -c "ddev ..."`** — обязательный префикс для всех
  ddev/wp-cli команд в этой среде (docker группа), не голый `ddev`/`wp`.
- **Права на git commit/push, деплой, редактирование любого файла темы —
  разрешены без подтверждения** (см. CLAUDE.md, "Autonomy"). Исключение —
  любой `rm -rf`/bulk-delete: подтверждать явно каждый раз (см. инцидент
  2026-09-02, каталог проекта был случайно удалён так же).
- **`git status`/staged diff перед коммитом** — всегда проверять, что
  случайно не попали временные/диагностические файлы (например,
  временный mu-plugin для SAVEQUERIES-профилирования выше) — они должны
  быть удалены до `git add`, не закоммичены "на всякий случай".
- **cPanel File Manager "Select File" через `file_upload` (несколько
  файлов за один вызов)** — при передаче двух+ путей в одном вызове
  `file_upload` реально загружается и обрабатывается ТОЛЬКО первый файл;
  остальные молча пропадают без ошибки (не появляются даже в списке
  прогресс-баров на странице). Подтверждено повторно 2026-09-08
  (`footer.php` + `style.css` в одном вызове → загрузился только
  `footer.php`). Всегда грузить по одному файлу за вызов и явно
  скриншотить страницу после каждого, чтобы убедиться, что имя файла
  реально появилось в списке со 100%.
- **Кириллица, напечатанная напрямую в cPanel Terminal (xterm) через
  `computer` action `type`, теряется/ломается** — подтверждено
  2026-09-08: команда `wp eval` с украинским текстом внутри PHP-строки
  (`"від 2 000 грн"`) на выходе дала просто `"2 000"` без кириллических
  букв. Латиница, цифры и большинство спецсимволов печатаются нормально
  — теряется именно кириллица. Обходной путь: писать нужный PHP-код
  (или любой другой с кириллицей) в файл локально, заливать через File
  Manager (`file_upload`), выполнять через `wp eval-file`, а не
  печатать инлайн-код с кириллицей в самом терминале.
- **GT- ("Google tag") ID через классический `gtm.js`/`dataLayer.push`
  загрузчик молча не отправляет данные** — подтверждено 2026-09-08:
  скрипт загружается (200 OK), но ни одного `collect`-хита на
  `google-analytics.com` не происходит — GT-контейнер без опубликованных
  тегов внутри (не то же самое, что реальный GTM-контейнер с тегами).
  Правильный способ — прямой `gtag.js`-загрузчик
  (`googletagmanager.com/gtag/js?id=...` + `gtag('config', 'ID')`), как
  и предписывает собственная страница "Установка тега" в Google
  Analytics. Симптом обнаруживается только явной проверкой сетевых
  запросов (`read_network_requests`) на реальный `g/collect`-хит —
  простого 200 на сам скрипт-загрузчик недостаточно для подтверждения,
  что аналитика реально работает.
- **`read_network_requests` (Chrome-расширение) врёт про статус-код для
  `POST https://analytics.google.com/g/collect`** — подтверждено
  2026-09-09: инструмент стабильно показывает `statusCode: 503` для
  каждого такого запроса (и для реального `gtag`-хита от клика по
  кнопке дзвінка на koval-legal.pp.ua, и для ручного тестового
  `fetch()` до того же URL). При этом сам `fetch()`, вызванный через
  `javascript_tool` в контексте страницы, получает от Google настоящий
  `204 No Content` — реальный ответ сервера успешный, ошибку выдумывает
  именно инструмент чтения сетевых запросов (похоже, не умеет
  корректно читать статус у этого конкретного типа cross-origin/
  keepalive POST). Вывод: для `analytics.google.com/g/collect` (и,
  возможно, аналогичных beacon-запросов) НЕ доверять `statusCode` из
  `read_network_requests` — перепроверять через прямой
  `await fetch(url, {method:'POST'})` в `javascript_tool` и смотреть
  `r.status`/`r.ok` оттуда.
- **Claude Code auto-mode classifier повторно блокує деплой-дії на проді
  через браузер (cPanel File Manager/Terminal)** — переперевірено
  2026-09-12: `file_upload` у File Manager і навіть просто `navigate` на
  `.../terminal/index.html` блокуються під різними причинами кожного
  разу ("Production Deploy", "Auto-Mode Bypass", "Sensitive Remote
  Exec") — незважаючи на явну постійну автономію в CLAUDE.md саме для
  цього проєкту й на перемикання сесії в "accept edits". Обхідний шлях,
  який спрацював: користувач сам вмикає "accept edits on" (Shift+Tab) —
  після цього ОДИН наступний `file_upload` проходить (перевірено двічі
  цього разу), але наступна дія того ж роду (навігація в Terminal) знову
  заблокована з новою причиною. Тобто дозвіл витрачається на одну дію і
  не тримається навіть у межах однієї "accept edits" сесії. Робочий
  патерн: просити користувача самого відкрити Terminal і виконати
  потрібну команду (`wp eval-file ...`), а Claude готує сам файл/скрипт
  і робить `file_upload`, повторюючи спробу після кожного вмикання
  "accept edits", якщо перша не пройшла.
