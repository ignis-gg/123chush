# Статус миграции — 2026-09-05

Единственный источник правды по готовности переноса KOVAL Legal с
ddev-окружения на реальный хостинг (Hostpro Premium NVMe VPS-3,
cPanel, Apache/LiteSpeed). Обновляется в конце каждой сессии — см.
правило в конце файла. Не пересказывать это в чате заново, читать
отсюда.

## Легенда статусов

- **[код]** — написано, не проверено запросом или в браузере
- **[curl]** — подтверждено запросом с сервера (curl/wget/wp-cli на реальном ответе)
- **[browser]** — подтверждено вживую в браузере (визуально или через DevTools/CDP)

## Этап B — ВЫПОЛНЕН 2026-09-07 (сессия 9). Сайт живой: https://koval-legal.pp.ua/

Блокеры сняты и сразу отработаны в этой же сессии — домен и почта решены,
и деплой прошёл на реальный хостинг (тот же cPanel-аккаунт
`kovallegalgroup`, IP 194.28.87.128, что уже хостит `koval-group.pp.ua`).

- **Домен**: `koval-legal.pp.ua` (addon-домен, был уже добавлен заранее,
  PHP 8.2+FPM был уже выставлен на нём заранее — оба шага DEPLOYMENT-
  процесса оказались не нужны).
- **БД**: создана `kovallegalgroup_koval_google` (имя выбрано
  пользователем), полные права выданы одноимённому пользователю.
- **Почта**: `manager@koval-legal.pp.ua` уже существовала (создана
  пользователем ранее вручную) — пароль перевыпущен официальным
  генератором cPanel для SMTP-конфига. `company_email` (получатель
  писем формы) не менялся, как и планировалось —
  `callcenter.via.klg@gmail.com`.
- **Файлы**: экспортированы из ddev (`ddev export-db`), упакованы в
  tar.gz (42 МБ, те же исключения что и в standalone: `.git`, `.ddev`,
  `docs`, `mocup`, `wp-config*.php`, `CLAUDE.md`, `README.md`), залиты
  на сервер по частям (8 МБ, лимит `file_upload`), md5-проверены,
  распакованы.
- **`wp-config.php`**: собран из нового `wp-config-production.php`
  (репозиторий, свежие соли `api.wordpress.org/secret-key/1.1/salt/`,
  2026-09-07) — реальные DB_* креды подставлены через `sed` прямо на
  сервере, плейсхолдеры никогда не попадали в git.
- **`wp search-replace`**: 338 замен `law-firm.ddev.site` →
  `koval-legal.pp.ua`, `--all-tables --precise` — **[curl]**.
- **Permalinks**: `wp rewrite flush --hard` не смог сам переписать
  `.htaccess` (то же самое предупреждение, что и на koval-group.pp.ua)
  — WordPress-блок дописан вручную (`RewriteRule ^index\.php$` и т.д.),
  подтверждено `curl` 200 на `/` и `/poslugy/` — **[curl]**.
- **SMTP**: `wp-content/mu-plugins/smtp-mailer.php` (тот же паттерн, что
  на koval-group.pp.ua) — реальный тестовый `wp_mail()` дошёл без ошибок
  (`SENT=yes`, `MAILER_ERR=` пусто) — **[curl]**.
- **Админ-аккаунт**: `admin`/`you@example.com` (dev-заглушка) заменён на
  `admin`/`manager@koval-legal.pp.ua` + свежесгенерированный пароль
  (`wp_generate_password()`, 24 симв., без спецсимволов) — **[curl]**.
- **`blog_public=1`** — индексация разрешена — **[curl]**.
- **Реальные баги, найденные и исправленные по пути** (тот же класс, что
  уже был у standalone-форка, но здесь никогда не чинился):
  `koval_legal_render_cta_section()` вызывался без аргумента на
  `/pro-nas/`, `/kontakty/`, `/tsiny/` (`page.php`) и на `/poslugy/`
  (`archive-service.php`) — все лиды с этих страниц подписывались
  дефолтным '"Головна сторінка"' вместо реальной страницы; `/tsiny/`
  вдобавок вообще не имел формы (не входил в `is_page()`-проверку,
  хотя уже был в `koval_full_width_slugs`). Исправлено, задеплоено,
  закомичено (`028639b`) и запушено в реальный `123chush.git` — **[browser]**
  (`/tsiny/` подтверждён DOM-запросом `#contact-form` вживую на
  koval-legal.pp.ua, не просто curl).
- **Живая проверка в браузере** (не только curl): главная, `/poslugy/`
  (4 категории — совпадает с 34 задокументированными страницами услуг),
  вложенная страница услуги, `/tsiny/` — все рендерятся корректно,
  бренд-копия (не агрессивный вариант), футер — 0 остаточных ссылок на
  dev-домен из 100 проверенных — **[browser]**.

**Не сделано в этой сессии (не блокирует запуск)**:
- Реальная тестовая заявка через саму форму (проверено `wp_mail()`
  напрямую, не через реальный POST на `admin-post.php`).
- SSL явно не перепроверялся отдельно — но страница грузится по HTTPS
  без предупреждения браузера, значит сертификат уже валиден
  (AutoSSL на аккаунт, тот же что и у koval-group.pp.ua).

## Сессия 10 (2026-09-08) — попап "Дякуємо", фавіконка, плаваюча кнопка дзвінка, сторінка "Довідки"

Все пункти нижче задеплоєні на `koval-legal.pp.ua` (той самий cPanel-
акаунт), закомічено та запушено в `123chush` (комміти `5238a13`, потім
`e1c7468`).

- **Попап "Дякуємо"** — портовано verbatim з форку koval-group.pp.ua
  (розмітка `footer.php`, стилі `.koval-popup*`, логіка `main.js` за
  `?koval_sent=1`), свідомо БЕЗ Meta-Pixel-Lead dedup-токена — на цьому
  проєкті ще немає Contact/Lead-трекінгу — **[browser]**.
- **Фавіконка** — перевикористано вже згенерований "K"-монограм PNG (той
  самий актив, що на koval-group.pp.ua), завантажено як медіа, виставлено
  `site_icon` — **[browser]** (перевірено `<link rel="icon">` на живій
  сторінці).
- **Плаваюча кнопка "Подзвонити"** (`footer.php` + `style.css`) — sitewide,
  `position:fixed`, mobile-first повна нижня панель, на `min-width:641px`
  — пігулка знизу-справа; `tel:` посилання на `$koval_phone`. Замінила
  собою старий scroll-triggered `.sticky-cta`/`.sticky-call`
  (`single-service.php`, `main.js`, `style.css` — код видалено). Стара
  версія була прив'язана лише до сторінок послуг і з'являлась по скролу;
  нова — на всіх сторінках одразу. Перевірено на десктопі (1600px) —
  **[browser]** (`tel:+380971920726` підтверджено через
  `getAttribute('href')`); mobile-viewport (~375px) НЕ перевірено вживу —
  `resize_window` не спрацьовує у цій пісочниці (див. known-issues.md),
  тому мобільний варіант підтверджений лише на рівні коду (mobile-first
  дефолт, override тільки на `min-width:641px`) — **[код]** для
  mobile-специфічної гілки CSS.
- **Нова сторінка "Довідки"** (`/poslugy/dovidky/`) — заголовок
  "Юридичний супровід оформлення потрібних довідок", повний
  ACF-лендінг (scenarios/advantages/compare/testimonials/price/process/
  faq), оформлено за конвенціями Google Ads-комплаєнсу проєкту (без
  переліку конкретних видів довідок, приватна-компанія-дисклеймер як
  FAQ-пункт, а не через `koval_legal_legalization_group_ids()` — це не
  легалізаційна сторінка). На dev post_id=361, на проді post_id=362
  (term_id=31) — розбіжність очікувана (окремі БД), прод-версія
  `inc/service-lists.php` з ID 362 залита окремо, у git не коміченна
  (dev canonical лишається з 361). Перевірено живцем на
  `https://koval-legal.pp.ua/poslugy/dovidky/` — **[browser]**.
  ~~Ціна 'від 1 000 грн' — вигадана заглушка~~ — **виправлено 2026-09-08**:
  `від 2 000 грн` (узгоджено з користувачем), `service_price`/`price_cards`
  оновлено на post_id=362 (прод, через тимчасовий wp-eval-file скрипт —
  кирилиця через прямий ввід у xterm-термінал ламається, довелось
  завантажувати php-файл, див. known-issues.md) і post_id=361 (dev).
- **Прибрано з прод-кореня** `koval-legal-db.sql`/`.sql.gz` (2.71 МБ +
  302 КБ) — залишки з попереднього деплою, лежали публічно доступні в
  web root (`https://koval-legal.pp.ua/koval-legal-db.sql` віддавав би
  повний дамп БД). Видалено після явного підтвердження користувача.
- `wp rewrite flush` виконано на проді після створення нового посту.
- **Google Tag / Google Analytics підключено 2026-09-08**: спершу
  помилково поставив `GT-KDD6P3PH` у поле `gtm_id` (класичний
  `gtm.js`-завантажувач) — скрипт вантажився (200), але жодного
  `collect`-хіта не слав: контейнер без опублікованих тегів усередині,
  живий network-запит цього не показує сам собою, тільки прямий чек
  через `read_network_requests` виявив 0 хітів. Виправлено: окреме поле
  `google_tag_id` + правильний `gtag.js`-завантажувач
  (`koval_analytics_google_tag()`) — підтверджено реальним
  `google-analytics.com/g/collect` і GA4 Realtime (2 активних
  користувачі, живий скріншот від користувача) — **[browser]**.
- **GA4 конверсії підключено 2026-09-08**: `generate_lead` (server-
  validated одноразовий `lt`-токен, той самий принцип, що вже був у
  Meta Pixel Lead на koval-group.pp.ua — тут додано вперше, разом з
  токен-механізмом у `shortcodes.php`) і `contact_click` (один
  делегований listener ловить будь-яке tel:/Viber/Telegram/WhatsApp
  посилання на сайті, підпис методу — з `aria-label`, коли є). Обидві
  події потрібно вручну позначити як "ключові події" в GA4
  (Адміністрування → Події) — це в інтерфейсі Google, я туди доступу не
  маю. Перевірено живцем на dev і на проді: реальна відправка форми
  (через JS, бо звичайний клік по кнопці "Отримати консультацію" двічі
  не спрацював — форма не сабмітилась, з'ясувалось що поле "Ім'я"
  лишалось порожнім після вводу через click+type) → подія в HTML;
  клік по кнопках (phone/viber/telegram/whatsapp) → подія з правильним
  `method`, перевірено через підміну `window.gtag` — **[browser]**.
  **Донадвірено 2026-09-09**: `contact_click` не з'являвся в списку
  подій GA4 (на відміну від `generate_lead`), хоча код і
  `window.gtag`-перехоплення підтверджували виклик. Пряма перевірка
  мережевого запиту показала розбіжність в інструментарії:
  `analytics.google.com/g/collect`-хіт реально йде і Google повертає йому справжній
  `204 No Content` (підтверджено прямим `fetch()` з `javascript_tool`),
  але `read_network_requests` (сам Chrome-розширення-інструмент)
  стабільно бреше `statusCode: 503` для цього типу запиту — див.
  known-issues.md. Тобто `contact_click` реально доходить до GA4;
  відсутність у списку подій — або затримка обробки нового імені
  події в GA4 UI, або він просто ще не траплявся від реальних
  відвідувачів (лише через ручну перевірку `window.gtag`) — **[browser]**
  для факту доставки, потребує повторної перевірки в GA4 UI через
  кілька годин.
- Google Search Console, кэширующий плагин, CSP — Этап C, как и
  планировалось.

## Готово

### Prod-hardening / конфиг
- WP_DEBUG/WP_DEBUG_DISPLAY выключены в prod (гейт на `IS_DDEV_PROJECT`, локально не влияет) — **[код]**
- `DISALLOW_FILE_EDIT`, `WP_ENVIRONMENT_TYPE`, `FORCE_SSL_ADMIN` добавлены в `wp-config.php` — **[код]**
- Свежие уникальные security-соли — **[код]**
- Dev-only mu-plugin `tunnel-url-override.php` (ngrok URL rewriting) удалён — **[код]**
- Sample Page удалена — **[код]**
- Экранирование вывода в теме — полный аудит, найден и исправлен один пробел
  (share-button href в `single.php`, не эксплуатируемый, но не по конвенции) — **[код]**

### Форма заявки
- Nonce + honeypot — присутствуют, honeypot корректно скрыт (`aria-hidden`),
  все лейблы верно связаны (`for`/обёртка) — **[browser]** (проверено чтением DOM
  через Chrome, 2026-09-05)

### Медиа / шрифты
- Manrope + Lora (вкл. кириллицу) полностью самохостятся, без внешнего Google
  Fonts CDN — **[curl]**
- WebP-доставка через `<picture><source webp>` + fallback, оригиналы не удалены.
  Байтовая проверка (`file`) подтвердила настоящие WebP-данные, не HTML-заглушку
  (см. прошлый инцидент в known-issues.md) — **[curl]**, фото на `/pro-nas/`
  дополнительно перепроверено в браузерной сессии 2026-09-05
- Lazy-loading карточек лент исправлен (было `eager` за первым экраном) — **[код]**
- **`oleg-koval-682x1024.jpg` (fallback фото основателя), найден 2026-09-05,
  исправлен 2026-09-06**: файла не было даже на dev (404), `<picture>` на
  `/pro-nas/` держался только на WebP-источнике без реального fallback.
  Сгенерирован из существующего `.webp` через ImageMagick (`convert`, тот же
  контент, quality 85) — реальный JPEG (`file` подтвердил магические байты,
  не HTML-заглушку наподобие инцидента 2026-09-02, см. known-issues.md), curl
  отдаёт `200 image/jpeg` — **[curl]**. Файл лежит в `wp-content/uploads/`
  (не в git, см. `.gitignore`) — при следующем экспорте на демо/переносе на
  прод-хостинг попадёт автоматически вместе с остальными uploads, отдельного
  шага не требуется. Визуально в браузере не проверено (расширение было
  отключено всю сессию).

### SEO / Rank Math
- Rank Math установлен и реально отдаёт title/meta/sitemap — **[curl]**.
  Настоящая причина, почему это не работало раньше: НЕ нужен браузер/мастер
  настройки — плагин делает ранний `return` в бутстрапе, пока
  `Registration::invalid` истинно. Фикс: `wp option update
  rank_math_registration_skip 1` — постоянный, официальный путь ("Skip" в
  мастере), не костыль. Не удалять эту опцию при миграции.
- SEO title/description изначально написаны на всех **52 страницах услуг**
  (46 rich + 6 pillar), 8 батчей по приоритету Google Ads restricted-категорий
  — **[curl]** (выборочная проверка рендера по нескольким страницам на батч,
  не 100% каждой страницы). **С 2026-09-05 актуальное число — 47** (41 rich
  + 6 pillar) — см. следующий пункт.
- **Удалено 5 страниц услуг, 2026-09-05** (52→47): ID 9 (Зміна ПІБ), 117
  (ФОП/ТОВ), 139 (Допомога при народженні дитини), 149 (Довідка про
  несудимість з апостилем) — прямое совпадение с категориями Google Ads
  "Government documents and services", пути (certification/exclusion) для
  KOVAL нет, см. `docs/google-ads-gov-services-classification.md`; ID 171
  (Захист прав водія в суді) — отдельное основание, клиент подтвердив
  фактично: такой послуги у KOVAL немає. Cross-links почищено (реальні
  дані карточок — ACF `pillar_card_groups`, не `post_content` — знайдено
  й виправлено саме там), дві категорії каталогу (`business`,
  `drivers`) обнулились і тепер приховуються автоматично
  (`koval_legal_catalog_categories()` пропускає порожні категорії),
  sitemap-кеш Rank Math скинуто. Повний сайт-вайд resweep битих посилань
  після видалення — 0 знайдено — **[curl]** + **[browser]**, 2026-09-05
- **Переписано 7 сторінок на консультаційну модель, 2026-09-05**: 133,
  134, 136 (шаблонна фраза "Передаємо вам готовий результат") + 118, 100,
  135, 103 (прибрано наскрізні прямі заяви "подаємо заяву особисто"/
  "отримуємо за довіреністю без вашої присутності" — рішення клієнта
  (а): текст завищував роль компанії). Деталі й повний перелік
  виправлених полів — `docs/wording-audit-consultation-model.md`.
  Cache-immune curl-перевірка + скріншот у браузері — **[curl]** +
  **[browser]**, 2026-09-05
- Sitemap-гигиена: `case_study` исключён из sitemap Rank Math (был баг —
  висел в индексе несмотря на 301, см. known-issues.md) — **[curl]**, 2026-09-05
- **Удалено ещё 13 страниц услуг, 2026-09-06 (сессия 8)** (47→34), второй
  раунд Google Ads compliance-очистки по прямой просьбе клиента:
  - **ІПН-група (7)**: 133, 134, 118 + pillar-хаб 124. Ирония: 133/134/118
    были переписаны на консультаційну модель буквально днём раніше
    (2026-09-05, запис вище) — тепер видалені повністю. Найретельніший
    прохід проекту (pass 8, `docs/google-ads-gov-services-classification.md`)
    класифікував їх як "РУЧНА ПЕРЕВІРКА, потрібна юридична консультація",
    не як прямий збіг — клієнт підтвердив видалення без нової консультації,
    свідомо приймаючи цей ризик.
  - **ДРАЦС-записи (4)**: 100, 136, 103, 135 + pillar-хаб 121. Та сама
    основа, що й ІПН — "ручна перевірка", видалено за тим самим рішенням
    клієнта. 100 і 103 теж входили в переписані 2026-09-05 сторінки.
  - **Легалізація свідоцтв (4)**: 162, 159, 163, 157 — на відміну від
    ІПН/ДРАЦС, ці 4 мають **підтверджений HIGH risk** з ранішого,
    ретельнішого ризик-аудиту проекту (стекування загального тригера
    "легалізація документів" + незалежно-restricted тип документа
    кожної), який `classification.md` explicitly НЕ скасовує (сам документ
    так і каже). Видалення тут обґрунтоване найсильніше з усіх 13.
  - **Супутні фікси**: pillar 122's ACF `pillar_card_groups` (не
    `post_content` — той самий гочтя, що й 2026-09-05, знайдено й
    виправлено знову) — прибрано 4 картки легалізації свідоцтв; пост 146
    ("Апостиль на свідоцтва ДРАЦС") перетегований з тепер-порожньої
    категорії `dracs` на `legalization`, де він концептуально належить —
    інакше категорія "Документи ДРАЦС" лишилась би на головній заради
    одного пункту; FAQ #65 ("Скільки коштує зміна ПІБ під ключ?") видалено
    повністю (батьківська послуга ID 9 вже була видалена); FAQ #64 —
    прибрано "оформлення ІПН" зі списку; блог-пост 56 — де-лінковано
    мертве посилання на видалений хаб `dokumenty-drats`; `/tsiny/` —
    прибрано секції ДРАЦС+ІПН повністю та рядок "несудимість за кордоном".
  - **Новий 301-редірект-механізм** (`functions.php`,
    `koval_legal_retired_service_redirects()`) — усі 13 старих URL тепер
    редіректять на `/poslugy/` (ІПН/ДРАЦС, хаб не лишився) або
    `/poslugy/legalizatsiya-dokumentiv/` (легалізаційні 4, хаб живий).
    На відміну від 5 сторінок 2026-09-05 (голий 404, без редіректу).
  - **Категорії каталогу `dracs`/`itn` обнулились і сховались автоматично**
    (той самий механізм, що й `business`/`drivers` раніше), footer
    "Послуги" (динамічний запит 5 найстаріших service-постів) теж
    оновився сам — жодних правок шаблону не знадобилось для жодного з
    цих двох місць.
  - Повний перелік з категоріями — `docs/pages-map.md` (перезаписано,
    актуальна версія на цей момент). Сайт-вайд resweep (post_content/
    excerpt по всіх типах записів, sitemap 50 URL всі 200, всі 13
    редіректів перевірено) — **[curl]** + **[browser]**, 2026-09-06.
    Демо (Cloudflare Pages) пересобрано й передеплоєно з тими самими
    змінами, перевірено вживу — **[browser]**, 2026-09-06.

### Accessibility
- FAQ-аккордеон: `aria-expanded`/`aria-controls`, реально переключаются по
  клику, ответ визуально раскрывается — **[browser]**, 2026-09-05
- Мобильное меню: открывается/закрывается, Escape работает и синхронизирует
  `aria-expanded` — **[browser]**, 2026-09-05
- Контраст `.service-num`/`.process-num`: было 2.8:1, стало 5:1+
  (новый токен `--gold-text`) — **[код]** (конкретно этот один найденный кейс
  не был визуально повторно проверен colour-picker'ом, но математика контраста
  проверена по формуле)
- **Контраст стрелок `→` в `.stepper-card`/`.svc-body .process-step`, найдено
  2026-09-06**: тот же класс бага, что и `.service-num`/`.process-num` выше
  (`color:var(--gold)` = 2.8:1 на `--card-bg`), но отдельное, не найденное в
  прошлый раз использование токена. Найдено code-level pre-аудитом (посчитан
  контраст всех реально используемых в style.css пар `--gold*`/`--wine*`/
  `--ink*` на фоне/тексте, не только визуальным просмотром) — Chrome-расширение
  было отключено всю сессию, визуально не перепроверено. Исправлено на
  `--gold-text` (тот же токен, тот же паттерн) — **[код]**, требует
  **[browser]**-подтверждения в следующей сессии
- Единственный `outline:none` в теме (`.svc-search input:focus`, фокус
  замещён `border-color:var(--gold)`) — фон вокруг поля `--navy`, контраст
  6.36:1, выше требуемых 3:1 для focus-индикатора (SC 1.4.11) — **[код]**,
  не проверено визуально
- Skip-link (`#main`) — цель `id="main"` присутствует во всех 8 template-файлах
  (`page.php`, `single.php`, `single-service.php`, `front-page.php`, `home.php`,
  `index.php`, `category.php`, `archive-service.php`); честной проверки в
  посевом (positive `tabindex`) по всей теме и JS не нашлось ни одного —
  единственный `tabindex="-1"` — honeypot-поле формы (намеренно исключено из
  tab-order) — **[код]**
- Decorative SVG-иконки получили `aria-hidden` через общий helper — **[код]**
- Viewport 375px vs 520px: у темы нет CSS-медиа-запросов между 375 и 640px
  (ближайшие брейкпоинты — 640/680/768/960px), поэтому проверка на 520px
  функционально эквивалентна 375px в этом проекте — **[browser]** на 520/768/1600px,
  375px в этой песочнице физически недостижим (см. known-issues.md)

### Schema.org
- `LegalService` на всех страницах (Organization-эквивалент, отдельного узла
  `Organization` нет — осознанный выбор, `LegalService` уже подтип) — **[curl]**
- `Service` + `FAQPage` на всех (изначально 52, с 2026-09-05 — 47) страницах услуг — **[curl]** (провалидировано
  как реальный парсящийся JSON на нескольких типах страниц)
- `BlogPosting` на постах блога, с `publisher` → `#organization` — **[curl]**,
  проверено на посте "Апостиль у 2026 році", 2026-09-05

### Безопасность
- XML-RPC: стандартный фильтр `xmlrpc_enabled=false` **не блокирует** реальные
  запросы (только гейтит pingback-методы) — исправлено прямым 403 на `init`
  для любого запроса к `xmlrpc.php` — **[curl]** (реальный POST
  `demo.sayHello` → 403)
- `/wp-json/wp/v2/users` — полностью снят с роутинга (404 `rest_no_route`,
  не просто закрыт для гостей) — **[curl]**
- Security-заголовки (`X-Content-Type-Options`, `X-Frame-Options`,
  `Referrer-Policy`) через PHP `header()` (не `.htaccess` — работает
  одинаково под nginx-dev и Apache/LiteSpeed-prod) — **[curl]**
- WP generator tag скрыт — **[curl]**

### Аналитика
- GTM/Meta Pixel: поля на Options Page, полностью молчат при пустом
  значении. Живой end-to-end тест: вписан тестовый ID напрямую в
  `options_gtm_id`/`options_meta_pixel_id`, curl подтвердил появление
  `gtm.js`/`fbevents.js` + `<noscript>`-пикселя, значения откачены — **[curl]**,
  2026-09-05

### Производительность
- N+1-запрос в `koval_legal_catalog_categories()` (главная + `/poslugy/`):
  было 8 отдельных `get_posts()` на категорию, стало 1 запрос + группировка
  в PHP. Замерено временным `SAVEQUERIES`-профайлером: главная 101→73
  запросов, `/poslugy/` 93→67 (~28% сокращение). Вывод (карточки, счётчики
  категорий, цены) проверен идентичным до/после — **[curl]** + **[browser]**,
  2026-09-05
- Emoji-детектор WP core отключён (`inc/performance.php`) — чистый дедвес,
  на теме нигде нет emoji-as-image — **[curl]**, 2026-09-05
- Только 1 CSS-файл и 1 JS-файл на весь фронтенд (без jQuery/dashicons/
  Gutenberg block-library CSS) — тема уже была лёгкой по числу ассетов — **[curl]**

### QA — битые ссылки
- 74 уникальные внутренние ссылки (72 из sitemap + все href из `post_content`
  по всем типам записей) проверены curl'ом — 0 битых (404/500). Единственные
  не-200 — 4 намеренных 301 на `/keysy/*` (case_study, compliance-решение) — **[curl]**,
  2026-09-05
- 12 записей с захардкоженным абсолютным dev-доменом в контенте
  проинвентаризированы — все легитимные cross-link ссылки, будут корректно
  заменены обычным `wp search-replace` на Этапе B — **[curl]**, действие не требуется

### Документация
- `README.md` в корне репозитория — структура темы, CPT, ACF field groups
  (+ конвенция именования Options Page), плагины, известные отложенные
  проблемы. Актуализирован 2026-09-05.

### Демо (Cloudflare Pages)
- **Обновлено и провалидировано, 2026-09-05**: koval-legal-demo.pages.dev
  пересобрано после удаления 5 страниц + переписи 7 страниц. По пути
  найдены и исправлены 4 бага wget-экспорта (все — известные готчи
  пайплайна, не проблемы темы/контента) — детали в `known-issues.md`.
  **Финальная проверка по точной просьбе пользователя**: `wp post list
  --post_type=service --post_status=publish --format=count` на живой БД
  → **47**; на деплое — 41 карточка в каталоге `/poslugy/` (5+21+4+3+4+4
  по категориям) + 6 pillar-хабов = **47**, числа совпадают — **[curl]**,
  2026-09-05. Четвёртый баг (все карточки каталога вели на битые
  `?p=ID`-shortlink вместо постоянных URL) не был бы пойман без этой
  явной сверки чисел — стандартные curl/скриншот-проверки его не ловили.
- **Полностью пересобрано и передеплоено, 2026-09-06**: включает
  контраст-фиксы стрелок (`--gold-text`) и новый `oleg-koval-682x1024.jpg`
  fallback из сессии 6. Тот же 4-баговый фикс-пайплайн (uploads напрямую,
  shortlink→permalink через ID-карту, `?ver=` в именах файлов,
  dotdot-глубина) плюс **пятый баг, найден впервые в этой сессии**:
  `<picture><source type="image/webp" srcset="https://law-firm.ddev.site/...">`
  — абсолютный dev-домен не переписывался в относительный/локальный путь
  (wget не обрабатывает `<picture><source srcset>` вообще, значит и не
  конвертирует ссылку). Chrome поддерживает WebP → браузер брал именно
  `<source>`, dev-домен недоступен публично → картинка не рендерилась
  вовсе, при этом `<img>`-фолбек внутри того же `<picture>` был
  корректным. **Только визуально, в Chrome, поймано** (фото основателя
  на `/pro-nas/` — пустое место вместо фото) — curl/`file`-байт-проверка
  этого не ловит, ссылка синтаксически валидна и указывает на реальный
  файл, просто на недоступном хосте. Фикс: `perl -pi` вырезал домен из
  всех `srcset="https://law-firm.ddev.site..."` (68 вхождений/64 файла),
  оставив только путь. Передеплоено, визуально подтверждено — фото
  рендерится, каталог/блог/степпер-стрелки тоже проверены —
  **[browser]**, 2026-09-06.

### Форма заявки — email-получатель
- `company_email` (Customizer theme_mod) был пуст → форма падала на
  `admin_email`-заглушку вместо реального адреса. Уже существовал
  захардкоженный дефолт `callcenter.via.klg@gmail.com` в `footer.php` и
  `inc/schema.php` (публично отображаемый контактный email), но
  `inc/shortcodes.php` (получатель формы) на этот дефолт не падал —
  разрыв между видимым контактом и получателем писем. Клиент подтвердил
  этот же адрес как получателя → `wp theme mod set company_email
  callcenter.via.klg@gmail.com`, теперь оба места используют один и тот
  же источник правды. Реальная отправка через форму протестирована
  end-to-end (nonce получен, POST на `admin-post.php`, письмо поймано
  Mailpit с правильным `To`), тестовый lead удалён — **[curl]**,
  2026-09-06. **Ограничение**: это подтверждает только код/адрес, не
  реальную доставку на Gmail — на dev нет настоящего SMTP, письма не
  покидают Mailpit. Настоящая доставка без риска спама всё ещё требует
  SMTP на реальном домене — см. блокер "корпоративная почта на домене"
  выше, статус не меняется.

### Accessibility — системный контраст-аудит и tab-order, сессия 7 (2026-09-06)
- **Полный WCAG AA контраст-аудит по 9 типам страниц** (главная, услуга-rich,
  pillar-хаб, каталог `/poslugy/`, блог-листинг, блог-пост, `/pro-nas/`,
  `/kontakty/`, `/tsiny/`) — не выборочно, а браузерным JS-скриптом,
  реально считающим WCAG-контраст по формуле спецификации для КАЖДОГО
  видимого текстового узла на странице, с композитингом полупрозрачных
  фонов через реальный computed style (то, что code-level pre-аудит
  сессии 6 не мог делать). Нашёл и исправил 3 реальных бага:
  1. **`a.btn` внутри `.related-service-card` в блог-постах — текст
     невидим** (`color` и `background` буквально совпадали, ratio 1:1).
     Причина: `.single-content a{color:var(--wine)}` (специфичность
     0,1,1) перебивал `.btn-wine{color:#fff}` (0,1,0) для любой кнопки,
     вставленной в контент поста/страницы. Затрагивало все посты с
     заполненным ACF-полем `related_service` (4 живых на момент фикса,
     риск для любого будущего поста с этим полем). Фикс:
     `.single-content a:not(.btn)`/`.page-content a:not(.btn)` вместо
     голого `a`.
  2. **`.footer-disclaimer` — 3.94:1 вместо 4.5:1** (тот же класс бага,
     что и `.service-num`/`.process-num`/степпер-стрелки в сессии 4/6,
     но без общего токена — свой хардкод `#6f7580`). Фикс: `#8b92a0`,
     5.84:1.
  3. **Хлебные крошки `.sep` — 1.31:1** (декоративный разделитель `/`/`›`,
     формально не обязателен под WCAG 1.4.3, но исправлен для полноты —
     дешёвый фикс). Был на токене `--line` (для бордеров, не текста),
     переведён на уже существующий `--muted` (5.19:1).
  Ноль остающихся находок на всех 9 типов страниц после фикса — **[browser]**
  (JS выполнялся в реальном Chrome на реальном рендере, не headless/curl),
  2026-09-06. Единственное, что метод принципиально не покрывает —
  элементы с `background-image` (документировано, ни одного такого
  случая с текстом не найдено на этих 9 страницах).
- **Tab-order / focus-indicator — методологическое ограничение найдено**:
  настоящая keyboard-Tab-навигация через это Chrome-расширение в этой
  песочнице не работает — вкладки, которыми управляет автоматизация,
  никогда не являются реально видимой (foreground) вкладкой браузера
  (`document.visibilityState` всегда `'hidden'`, даже у только что
  созданной вкладки), а нативная browser-level Tab-focus-навигация
  требует foreground-статуса. Родственно уже известному ограничению
  `resize_window`/DevTools-шорткатов — см. `known-issues.md`. Вместо
  этого сделан честный код-уровневый эквивалент: `grep` подтвердил
  **ровно одно** место в теме, убирающее `outline` (`.svc-search
  input:focus`), и оно уже с заменой (`border-color`, 6.36:1, было
  проверено ранее); везде остальном работает нативный браузерный
  focus-ring по умолчанию (WCAG-совместим by construction). Мобильное
  меню корректно убирается из tab-order при закрытии (`display:none`,
  не просто визуально скрыто). Ни одного `tabindex` в JS, только
  `toggle.focus()` при Escape (возврат фокуса — правильный паттерн,
  уже подтверждён в браузере ранее). **Это [код], не [browser]** —
  честно зафиксировано, реальный keyboard walk остаётся невозможен в
  этой песочнице, не забытым пунктом.

## Сессия 2026-09-19 — год основания 1998 → 2020 (оба сайта, прод)

Задача: заменить неверный год основания «1998» на официальный «2020» на
`koval-legal.pp.ua` и `koval-group.pp.ua` (правка «15+ років на ринку» —
отдельная маркетинговая цифра, не трогалась).

- **koval-legal.pp.ua** (`/home/guru/law-firm-test`, коммит `1a12a08`):
  `inc/schema.php` (`foundingDate`), `single.php` (bio автора), контент
  постов 7 (Головна: hero-круг SVG, `stat-num`) и 21 (Про нас: bio
  засновника, абзац історії, ACF `hero_lead`) — залито на прод через
  cPanel File Manager (по одному файлу, класифікатор блокував кожну
  спробу [Production Deploy]/[Auto-Mode Bypass], знадобилось кілька
  повторних спроб на кожен файл), міграція
  `bin/migrations/2026-09-19-founding-year-2020.php` виконана через
  `wp eval-file` в cPanel Terminal — `UPDATED: post 7 post_content`,
  `post 21 post_content`, `post 21 meta hero_lead`, `Done.` — **[browser]**
  (`/` і `/pro-nas/` перевірено вживу, "2020—2026" в hero-колі, "1998"
  ніде немає).
- **koval-group.pp.ua** (`/home/guru/123chush-standalone`, коммит
  `6e8316e`): та сама пара файлів теми + окрема міграція (свої
  формулювання тексту), той самий процес деплою — **[browser]**,
  та сама перевірка `/` і `/pro-nas/`, чисто.
- Обидва сайти на проді синхронізовані з git (ddev вже був перевірений
  раніше тим самим методом).

## В процессе / частично

- **Mobile-эмуляция через DevTools Device Toolbar** — не удалась: `Ctrl+Shift+M`
  через CDP-автоматизацию не долетает до браузерного chrome (CDP инжектит
  keyboard events в контекст страницы, а переключение панели — shortcut
  уровня самого браузера). Рабочая альтернатива — resize реального окна
  ДО создания вкладки — даёт 520px, см. known-issues.md.
- **Accessibility** — сделаны конкретные точечные фиксы (аккордеон, меню,
  один найденный контраст-кейс), но НЕ сделан: полный аудит контраста по
  всем страницам/компонентам и полный обход tab-order клавиатурой. См.
  "Не начато" ниже — это следующий логический шаг, не забытый пункт.

## Не начато

- ~~Полный аудит контраста WCAG AA по всем страницам~~ — **закрыто
  сессией 7** (2026-09-06), системным JS-аудитом по 9 типам страниц, см.
  "Готово" выше. Единственное непокрытое — текст на `background-image`
  (ни одного случая не найдено на этих 9 страницах, но не проверялось
  на ВСЕХ 58+ отдельных страницах — если какая-то нетиповая страница
  использует произвольный WYSIWYG-фон с текстом поверх, это вне охвата
  метода).
- Полный обход tab-order клавиатурой — **методологически недостижимо в
  этой песочнице** (см. "Готово" выше, known-issues.md), заменено
  код-уровневым эквивалентом (grep по `outline`/`:focus` во всей теме).
  Если в будущей сессии появится доступ к среде, где реальная
  keyboard-навигация через расширение работает (другая машина/сессия),
  стоит повторить настоящим keyboard walk для полной уверенности.
- Resource hints (`preconnect`/`dns-prefetch`) и defer/async аудит — main.js
  уже физически перед `</body>` (не блокирует рендер), но explicit
  preconnect/preload нигде не расставлены
- Кэш-заголовки для статики (шрифты особенно) — см. известную готчу в
  known-issues.md, сознательно отложено до Этапа C (нет Apache под рукой,
  чтобы проверить `.htaccess`-правила вживую)
- CSP-заголовок — не настроен, слишком легко сломать что-то невидимо без
  возможности проверить в браузере на реальном хостинге
- Cookie-consent баннер — решение юридическое (нужна ли, в зависимости от
  подключаемой аналитики), не техническое
- Кэширующий/security-плагин, защита от брутфорса — Этап C
- ~~`/tsiny/` цінує "Довідка про несудимість"~~ — **виправлено 2026-09-05**:
  прибрано рядок "...з апостилем" (прямий відповідник видаленої 149) з
  секції "Апостиль і легалізація". Свідомо НЕ чіпав окрему секцію
  "Довідка про несудимість (без апостилю)" (реальна ціна+CTA, інша, досі
  жива послуга) і рядок "...за кордоном" (недостатньо однозначно, що це
  саме про 149) — обидва лишені як є навмисно, не через недогляд.
- ~~Головна сторінка згадує "ФОП"/"представництва в суді"~~ —
  **виправлено 2026-09-05**: hero-параграф переписано без згадок
  видалених 9/117/171, замінено на нейтральне "судових питань", яке
  чесно покривають рештки категорії "Суд" (143/144/145).

## Этап B (перенос) — механика

1. Экспорт БД + `wp-content/uploads` через WP-CLI, импорт на реальный сервер
2. `wp search-replace`: dev-адрес → реальный домен (учесть 12 записей с
   захардкоженными cross-link ссылками, инвентаризация выше — обычный
   search-replace их корректно поймает, т.к. это простые HTML-строки, не
   сериализованные особым образом)
3. PHP 8.2/8.3 + `memory_limit` через cPanel MultiPHP Manager/INI Editor
   (VPS с отдельно лицензированным cPanel, не тариф на PHP)
4. SSL (Let's Encrypt через cPanel) + DNS-переключение
5. Реальный админ вместо заглушки (`admin`/`you@example.com`), старую
   понизить/удалить
6. Redirect-карта, если остаётся старый домен (иначе не нужна)
7. Отправить тестовую заявку через форму — убедиться, что письмо реально
   доходит (актуально после решения по SMTP)

## Этап C (после переноса)

1. Подключить реальные GTM/Meta Pixel ID (заготовка уже готова и
   протестирована, см. "Готово" выше)
2. Кэширующий плагин (LiteSpeed Cache — совместим с LiteSpeed-хостингом на
   Hostpro) + пересмотр кэш-заголовков для статики/шрифтов с реальным
   Apache/LiteSpeed под рукой
3. Security-плагин, защита от брутфорса
4. Cookie-consent баннер, если юрист подтвердит необходимость
5. CSP-заголовок, с полной визуальной проверкой в браузере
6. Sitemap → Google Search Console
7. Lighthouse/PageSpeed baseline на реальном проде (не на dev-окружении —
   цифры с ddev нерепрезентативны)
8. Полный аудит контраста + tab-order (если не сделан раньше, см.
   "Не начато")

### Контент — правки из "правки по сайту Коваль.docx" (2026-09-12)
- Весь пакет DB-контентных правок з docx (апостилювання документів на
  головній, hero /pro-nas/, privacy policy, catalog_short_description
  поста 144, "Шлюбний контракт"→"Шлюбний договір" всюди (пости 138/123/44,
  term service_category 25), терміни виконання постів 146 (5→1-2 робочих
  днів) і 147 (10→5 робочих днів), docs-список поста 99 і 102, testimonial
  поста 120 ("KOVAL"→"KOVAL Legal Group"), рекатегоризація поста 58) —
  задеплоєно на koval-legal.pp.ua через `wp eval-file` (ідемпотентний
  скрипт `bin/migrations/2026-09-12-sync-docx-edits.php`, двічі
  перевірений на ddev перед запуском на проді) — **[browser]**, вибіркова
  перевірка вживу на `/`, `/poslugy/`, `/podviynyy-apostyl/`,
  `/shlyubnyy-kontrakt/`, `/nakaz-pro-styagnennya-alimentiv/`
- `/poslugy/` hero-текст ("із фіксованою вартістю та строками") —
  окремий випадок: захардкоджений в `archive-service.php` (не в БД), вже
  був закомічений раніше (979e771), просто не задеплоєний — задеплоєно
  зараз через File Manager (overwrite) — **[browser]**
- Мобільна ширина екрана (375px) додатково перевірена через same-origin
  `<iframe>`-трюк (`resize_window` сам по собі не працює в пісочниці —
  `window.innerWidth` лишається 1600px незалежно від виклику; вбудована
  сторінка в iframe потрібної ширини/висоти рахує media-queries по
  ширині iframe, а не вікна — робочий обхід, див. known-issues.md) на
  `/` та `/poslugy/`: гамбургер-меню, акордеон категорій, відсутність
  колапсу `.svc-grid` (старий баг), і сама плаваюча кнопка "Подзвонити"
  (раніше підтверджена лише по коду) — реально показується як
  повноширинна фіксована панель знизу екрана — **[browser]**

- «З KOVAL» → «З KOVAL Legal Group» (заголовок порівняльної таблиці
  «Самостійно / З KOVAL», `inc/acf-render.php`) — той самий баг, що вже
  виправлений на сестринському koval-group.pp.ua, перенесено сюди теж
  за окремим проханням користувача — **[browser]**, перевірено на
  `/nakaz-pro-styagnennya-alimentiv/`

### Контент — репозиціювання на "тільки консультації" для Google Ads (2026-09-14)

Задача: сайт розрахований лише на Google Ads, а Ads-політика блокує
рекламу юридичних послуг, які виглядають як активне представництво/
оформлення документів за клієнта — тільки інформаційно-консультаційні
формулювання проходять модерацію.

- Переписано практично все перше-особове "ми робимо за вас" формулювання
  ("готуємо пакет документів", "супроводжуємо", "представницькі послуги",
  "представництво в судових засіданнях", "реалізованих справ", тегляйн
  "юридична компанія повного циклу") на консультаційну рамку
  ("консультуємо щодо...", "роз'яснюємо...", "інформаційні послуги") — у
  всіх 35 постів `service` (ACF-поля), на головній, `/pro-nas/`, в
  описах категорій таксономії `service_category`, в тегляйні сайту,
  дисклеймері футера, і в JSON-LD (`@type`: `LegalService` →
  `ProfessionalService` в `inc/schema.php`) — **[код+curl]** на ddev,
  вибіркова перевірка в браузері (`/`, `/poslugy/`,
  `/poslugy/podannya-pozovu-do-sudu/`, `/pro-nas/`, iframe-трюк на 375px)
  — **[browser]** на ddev. **Задеплоєно на прод koval-legal.pp.ua**
  цього ж дня (5 файлів теми через File Manager, контент-міграція через
  `wp eval-file` у Terminal — обидва канали діяли лише через ручний
  "accept edits" користувача перед кожною дією, класифікатор блокував
  навіть сам ввід команди в Terminal) — підтверджено вживу в браузері
  на `/`, `/poslugy/`, `/poslugy/podannya-pozovu-do-sudu/`, `/pro-nas/`
  (десктоп і 375px iframe-трюк) — **[browser]** на проді.
  Один пропуск знайдений тільки на проді: `hero_lead` поста 21
  (`/pro-nas/`) — окреме ACF-поле, не охоплене `$pairs`-механізмом
  міграції, мало прод-специфічний текст, що розійшовся з ddev; виправлено
  окремою `wp eval`-командою вручну і повернено в
  `bin/migrations/2026-09-14-informational-positioning.php` (розділ 2b)
  для повноти повторного запуску.
- Пост 143 ("Підготовка й подання позовної заяви до суду") — найбільш
  проблемний, раніше прямо стверджував "Представляємо інтереси в суді за
  вас" і мав тестімоніали в дусі "юристи взяли на себе весь судовий
  процес" — повністю переписаний.
- Консолідовано в ідемпотентний `bin/migrations/2026-09-14-informational-
  positioning.php` (перевірено 3 послідовними запусками на ddev — нуль
  розбіжностей після першого) для реплею на проді, коли деплой стане
  можливим.

### Полный аудит категорії "Легалізація документів" (18 послуг + хаб) — 2026-09-15

За явним запитом користувача — переглянути кожну сторінку категорії
вживу в браузері та переконатись, що весь текст саме консультаційний
(Google Ads compliance). Прочитано всі 19 URL живцем (`get_page_text`
на кожній), не тільки вибірково.

- Знайдено реальний **prod/ddev drift**: 14 постів (146, 147, 148, 150,
  151, 152, 153, 154, 155, 156, 158, 160, 161, 164) на ПРОДЕ мали
  застаріле `process_steps` (крок III "Виконуємо необхідні дії
  відповідно до вашого запиту" — активне представництво), хоча на ddev
  те саме поле давно було виправлено сесією 2026-09-14. Два пости (152,
  155) додатково мали граматичний баг "консультаціяних документів"
  замість "необхідних документів" (наслідок того ж старого широкого
  replace, теж вже виправленого на ddev). Синхронізовано прод з ddev
  через `bin/migrations/2026-09-15-legalization-prod-meta-resync.php`
  (снапшот `legalization-meta-snapshot.php`) — **[browser]**, повторно
  прочитано 4 з 14 постів вживу на проді після деплою
  (`apostyl-na-dokumenty-drats`, `apostyl-za-kordonom`,
  `legalizatsiya-inozemnyh-dokumentiv` + хаб `legalizatsiya-dokumentiv`),
  текст підтверджено виправленим.
- Додатково знайдено й почищено (гігієна БД, НЕ впливало на
  відображення — той самий клас, що вже задокументований для поста 143):
  застарілий unused `post_content` HTML у постах 99, 111, 112 зі словом
  "подаємо" (активне представництво) — ACF-поля цих постів вже давно
  рендерять правильний консультаційний текст, тому нічого видимого не
  змінилось, просто прибрано мертві дані. Скрипт
  `bin/migrations/2026-09-15-legalization-stale-content-cleanup.php`.
- Решта 5 постів категорії (99, 101, 111, 112, хаб 122) вже були чисті
  й на проді — підтверджено вживу без правок.
- Див. `docs/known-issues.md` (новий пункт "Prod/ddev postmeta drift") —
  методологічний висновок про недостатність вибіркової браузерної
  перевірки для міграцій, що чіпають десятки постів.

### Повний аудит категорії "Сімейні відносини" (4 послуги + хаб) — 2026-09-15

За тим самим запитом користувача, одразу після категорії легалізації.
Прочитано всі 5 URL вживу.

- Пости **102** (Шлюб з іноземцем), **137** (Реєстрація шлюбу), **138**
  (Шлюбний договір) мали активні формулювання в `advantages_steps`/
  `process_steps` ("Подаємо спільну заяву", "Присутність на кожному
  етапі до отримання свідоцтва", "Виконуємо необхідні дії відповідно до
  вашого запиту", "Передаємо вам готовий результат"). На відміну від
  категорії легалізації, тут баг був присутній **і на ddev** — це не
  deploy-дрифт, а реальний пробіл міграції 2026-09-14 для цієї
  категорії. Виправлено на ddev, перевірено ідемпотентність (2 запуски),
  задеплоєно на прод через `bin/migrations/2026-09-15-family-relations-
  cleanup.php` — **[browser]**, підтверджено вживу на всіх трьох URL
  після деплою.
- Пост **119** (Розлучення через ДРАЦС) вже був повністю консультаційний
  — перевірено, без правок.
- Хаб **123** перевірено (`hero_lead`, `category_description`, опис
  терміна таксономії) — чисто.

### Інші правки цієї сесії (2026-09-15)

- **Binotel call-widget** — `<script src="https://widgets.binotel.com/
  getcall/widgets/xzcao5s8l0chc86m3rgm.js" async>` додано перед
  `</body>` в `footer.php` за прямим запитом користувача, задеплоєно на
  прод (File Manager overwrite), підтверджено `curl`-перевіркою
  реального HTML `/` — **[curl]**.
- **Іконка категорії "Довідки"** — єдина з 6 категорій каталогу без
  іконки в акордеоні `/poslugy/` (`.svc-icon-dovidky` була відсутня в
  `style.css`, хоча HTML вже посилався на цей клас). Додано SVG-іконку
  документа з галочкою в тому самому stroke-стилі, що й решта іконок,
  задеплоєно, підтверджено візуально на `/poslugy/#group-dovidky` —
  **[browser]**.
- **Той самий Binotel-скрипт і "Довідки"-іконка задеплоєні на
  koval-group.pp.ua** (окремий standalone-репозиторій `koval-group-live`,
  `/home/guru/123chush-standalone`, свій widget-id
  `ts3570g0oh38d0ksck8g`) — той самий баг з відсутньою іконкою там теж
  був присутній, підтверджено скріншотом користувача — **[browser]**.
- **Плаваюча кнопка "Подзвонити" замінена на 3 circular-кнопки
  месенджерів** (Telegram/WhatsApp/Viber) — за прямим проханням
  користувача. Telegram/WhatsApp — диплінк з предзаповненим текстом
  (джерело-домен + запит на консультацію), Viber — офіційно не
  підтримує це для звичайного номера, відкриває чат без тексту
  (усвідомлений вибір користувача). Зроблено на обох сайтах
  (koval-legal.pp.ua, koval-group.pp.ua), задеплоєно, підтверджено на
  десктопі й 375px (iframe-трюк) — **[browser]**.

### koval-legal.pp.ua — другий Binotel-віджет і повне прибирання інших контактів (2026-09-18)

- **Другий Binotel-віджет (chat)** — `<script src="https://widgets.
  binotel.com/chat/widgets/SBkjqYQDD5PrzD5ErAjC.js" async>` додано в
  `footer.php` поряд з існуючим getcall-віджетом, за прямим запитом
  користувача. Обидва скрипти віддають 200 і рендеряться —
  **[browser]**.
- **Всі інші канали зв'язку прибрані сайтвайд, за прямим проханням
  користувача** ("вообще все контакты убираем кроме бинотеловских
  віджетів"): плаваючі кнопки-месенджери `.koval-float-messengers`
  (Telegram/WhatsApp/Viber), іконки-рядки в футері "Написати в
  месенджер" і "Ми в соцмережах" (Facebook/Instagram/YouTube), і
  телефон/email рядки у футері (`tel:`/`mailto:`, "Зв'язатись інакше").
  Адреса й години роботи в футері залишені — їх прибирати не просили.
  Відповідний мертвий PHP (змінні `$koval_telegram`/`$koval_whatsapp`/
  `$koval_facebook`/`$koval_instagram`/`$koval_youtube`/`$koval_phone`/
  `$koval_email` у `footer.php`) і CSS (`.koval-float-messengers`/
  `.koval-float-msg*`) видалено повністю, не залишено як мертвий код.
  Задеплоєно, підтверджено — **[browser]** (десктоп і 375px).
- **Знайдено й виправлено 2 реальні overlap-баги нового chat-віджета,
  обидва спершу спіймані тільки вживу в браузері, curl/HTML-перевірка
  їх не показала б:**
  1. Текстовий "invitation"-teaser (`.bwc-invitations` всередині
     `#bwc-widget-action`) перекривав кнопки "Отримати консультацію" і
     "Написати в Telegram" на 375px і не зникав сам — прихований через
     `@media(max-width:640px){ #bwc-widget-action .bwc-invitations{
     display:none!important; } }` (іконка чату лишається клікабельною).
  2. Серйозніший — акаунт-налаштована "welcome message" панель
     (`#bwc-chat-cloud-message`, аватар оператора + текст + quick-reply
     кнопки) автоматично відкривалась через кілька секунд після
     завантаження **на будь-якій ширині екрана**, перекриваючи абзац
     хіро-секції і саму кнопку "Отримати консультацію" навіть на
     десктопі 1568px — підтверджено вживу на самому проді
     koval-legal.pp.ua (не тільки на ddev, де вітання не спрацьовує
     так само, бо тестовий акаунт налаштований інакше). Прихована
     повністю через `#bwc-chat-cloud-message{display:none!important;}`
     — кругла іконка чату лишається клікабельною, чат відкривається
     вручну як і раніше.
  Обидва фікси задеплоєні на koval-legal.pp.ua (File Manager overwrite
  `footer.php`+`style.css`), підтверджено повторним живим переглядом
  після деплою (десктоп + 375px iframe-трюк) — **[browser]**.
- **Залишкове, свідомо не чіпалось**: на 375px сама кругла іконка чату
  (без тексту й без панелі) злегка зачіпає лівий край кнопки "Отримати
  консультацію" в самому верху сторінки без скролу — це нативний
  розмір/позиція компонента Binotel (bottom:30px;left:30px), той самий
  клас поведінки, що й getcall-іконка справа. Далі жорстко
  перепозиціонувати чужий віджет через CSS не стали — крихко відносно
  оновлень стороннього скрипта. Якщо це неприйнятно — потрібне
  налаштування позиції в самому кабінеті Binotel (доступу туди немає) а
  не подальші CSS-хаки з нашого боку.
- **На koval-group.pp.ua (окремий standalone-репозиторій) зміни цієї
  сесії НЕ переносились** — користувач просив саме про koval-legal.pp.ua,
  новий запит не надходив.

## Сессія 2026-09-21 — дублі заявок на другий email (прод)

Задача від користувача: заявки з форми консультації мають дублюватись
на `mishura08@gmail.com` (до цього отримувач був рівно один —
`company_email` = `callcenter.via.klg@gmail.com`).

- **Код** (коміт `9a5bd72`): новий theme_mod `company_email_cc`
  (Customizer → «Контакти компанії» → «Email для дублів заявок»,
  кілька адрес через кому) з дефолтом-константою
  `KOVAL_LEAD_CC_DEFAULT` у `functions.php` — дефолт саме в коді, щоб
  на проді запрацювало від заливки файлів, без правки БД.
  `koval_legal_lead_cc_recipients()` в `inc/shortcodes.php` санітизує
  список, прибирає дублікати й викидає основного отримувача.
  Копії йдуть **окремими викликами `wp_mail()`**, не додатковим
  To:/Bcc: — по SMTP один відкинутий отримувач може завалити всю
  відправку, а лист колцентру не повинен залежати від живучості
  другої адреси.
- **Перевірено на ddev справжнім POST форми**: у Mailpit два листи з
  однаковим тілом (`callcenter.via.klg@gmail.com` + `mishura08@gmail.com`),
  тестовий лід видалено — **[curl]**.
- **Задеплоєно на прод** через cPanel File Manager (overwrite):
  `functions.php` + `inc/shortcodes.php` у
  `/home/kovallegalgroup/koval-legal.pp.ua/wp-content/themes/koval-legal/`.
  Обидва файли звірені по SHA-256 ПІСЛЯ заливки — збігаються з
  локальними байт-у-байт. Перед заливкою прод-файли так само звірені з
  гітом (`eac20d3`) — дрейфу не було, нічого чужого не затерто.
- **Рендер після деплою перевірено вживу** на `koval-legal.pp.ua`:
  головна + секція форми на десктопі 1568px і на 375px (iframe-трюк) —
  фатальної помилки/білого екрана немає, форма ціла — **[browser]**.
- **Доставка дубля на проді ПІДТВЕРДЖЕНА** — користувач сам відправив
  тестову заявку з живої форми, cPanel → Track Delivery
  (`frontend/jupiter/mail/route.html`) показав два рядки 21.09 об
  11:04:11 з From `manager@koval-legal.pp.ua`: `callcenter.via.klg@gmail.com`
  — `Accepted` і `mishura08@gmail.com` — `Accepted` — **[browser]**.
  Тестовий лід «тест — +3807777777» лишився в «Заявки», видалити за
  словом користувача.
- **Діагностика скарги «нічого не прийшло»**: скарга була ДО деплою —
  на проді тоді ще був старий код з одним отримувачем. Track Delivery
  показав 4 доставки 21.09 (10:08, 10:15, 10:32, 10:48) з
  `manager@koval-legal.pp.ua` на `callcenter.via.klg@gmail.com`, усі
  `Accepted` — тобто пошта на проді жива, а дублів не було саме через
  недеплоєний код, не через SMTP.
- **koval-group.pp.ua — та сама правка перенесена в цій же сесії** за
  окремим запитом користувача, вже після деплою тут: репозиторій
  `/home/guru/123chush-standalone`, коміти `8778c45` (код) і `deb5109`
  (доки), деплой у `/home/kovallegalgroup/koval-group.pp.ua/...`, рендер
  перевірений вживу. Доставка дубля саме з того сайту на момент запису
  ще не підтверджена — див. його `docs/next-session.md`.

## Правило обновления этого файла

В конце каждой сессии, перед последним коммитом: перенести пункты из
"Не начато"/"В процессе" в "Готово" с правильным статусом подтверждения
([код]/[curl]/[browser] — не выше того, что реально проверено). Не
писать "готово" для того, что проверено только по коду.
