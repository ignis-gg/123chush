# Следующая сессия — что делать

Обновлено: 2026-09-08 (сессия 10). Этот файл перезаписывается каждой
сессией под актуальное состояние — не накапливать закрытые пункты.
Полная картина готовности — `docs/migration-status.md`, полный список
живых страниц — `docs/pages-map.md`.

## Главное: koval-legal.pp.ua живой, сессия 10 закрыта

**https://koval-legal.pp.ua/** — Этап B (перенос, сессия 9) и сессия 10
(попап "Дякуємо", фавіконка, плаваюча кнопка "Подзвонити", нова сторінка
"Довідки", прибрано публічно доступний дамп БД з прод-кореня) — все
задеплоєно та підтверджено вживу в браузері. Деталі — розділ "Сесія 10"
у `docs/migration-status.md`. Комміти: `5238a13`, `e1c7468` (обидва в
реальному `github.com/ignis-gg/123chush`).

## Приоритет 1 — доделать после этой сессии

- ~~Ціна "Довідки"~~ — **виправлено 2026-09-08**: `від 2 000 грн`
  (узгоджено з користувачем), `service_price` і `price_cards` оновлено
  на post_id=362 (прод) / 361 (dev).
- **GA4 конверсії підключено 2026-09-08**: `generate_lead` (реальна
  відправка форми, server-validated одноразовий токен — той самий
  принцип, що вже був для Meta Pixel Lead на koval-group.pp.ua) і
  `contact_click` (клік по будь-якому tel:/Viber/Telegram/WhatsApp
  посиланню на сайті, один делегований listener). Обидві події треба
  вручну позначити як "ключові події" в GA4 (Адміністрування → Події) —
  це в інтерфейсі Google, я не маю туди доступу. Перевірено живцем на
  проді: реальна відправка форми → подія в HTML; клік по кнопках →
  подія з правильним `method`.
- **Мобільний вигляд плаваючої кнопки "Подзвонити"** — код перевірено
  (mobile-first дефолт: повна нижня панель; `min-width:641px` override —
  пігулка), але НЕ підтверджено живим скріншотом на реальному мобільному
  viewport — `resize_window` не працює в цій пісочниці (див.
  known-issues.md). Якщо з'явиться доступ до середовища, де реальний
  resize спрацьовує — довірити.
- **Реальная тестовая заявка через саму форму** (не просто `wp_mail()`
  напрямую) — заполнить форму вживую на koval-legal.pp.ua, убедиться,
  что письмо реально приходит на `callcenter.via.klg@gmail.com`.
- Явно проверить SSL-сертификат отдельно (сейчас косвенно подтверждён —
  страница грузится по HTTPS без предупреждения браузера).

## Приоритет 2 — Этап C (после переноса, не блокирует)

1. ~~Подключить реальный GTM ID~~ — **готово 2026-09-08**: `gtm_id` =
   `GT-KDD6P3PH` (новий уніфікований "Google tag", сумісний з тим самим
   `gtm.js`-завантажувачем — офіційна пряма заміна GTM-контейнера, коду
   торкатись не довелось). Перевірено **[browser]**: реальний запит
   `googletagmanager.com/gtm.js?id=GT-KDD6P3PH` → 200, і на dev, і на
   проді. Пов'язаний GA4-потік — `G-LEV6HJZ19S` (сам ID нікуди не
   вставляється напряму, він "всередині" GT-тега). Реальні дані в GA4
   з'являться з затримкою — це нормально для щойно встановленого тега.
   Meta Pixel ID — досі не підключено, чекає на реальний ID від клієнта.
2. Кэширующий плагин (LiteSpeed Cache) + пересмотр кэш-заголовков для
   статики/шрифтов с реальным Apache/LiteSpeed под рукой.
3. Security-плагин, защита от брутфорса.
4. Cookie-consent баннер, если юрист подтвердит необходимость.
5. CSP-заголовок, с полной визуальной проверкой в браузере.
6. Sitemap → Google Search Console.
7. Lighthouse/PageSpeed baseline на реальном проде (не на dev).

## Не трогать / решено окончательно

- Домен и почта koval-legal.pp.ua — решение принято, не пересматривать
  без явного нового запроса пользователя.
- `git push` в этом проекте — `origin`'s push URL корректный
  (`https://github.com/ignis-gg/123chush.git`), проверено и в сессии 9,
  и в сессии 10. Если снова увидите push на `123chush-snapshot-*` — это
  регресс, чинить тем же способом (`git remote set-url --push origin
  https://github.com/ignis-gg/123chush.git`).
- Плаваюча кнопка "Подзвонити" замінює собою старий scroll-triggered
  `.sticky-cta`/`.sticky-call` — та логіка навмисно видалена
  (`single-service.php`, `main.js`, `style.css`), не відновлювати.

## Отдельный, нерешённый вопрос (не этот проект)

- **`kovallegalgroup.com.ua`** (окремий, попередньо існуючий прод-сайт на
  тому ж cPanel-акаунті) — користувач повідомив, що сайт "не
  відкривається" попри робочий хостинг/wp-admin доступ. DNS/NS/Cloudflare/
  хостинг-конфігурація виключені як причина (перевірено `.ua`-реєстром
  напряму, Cloudflare dashboard, dnschecker.org, тестом доступності
  1.1.1.1 — все ОК). Залишена гіпотеза — блокування на рівні
  домену/мережі (SNI-фільтрація або security-vendor blocklist).
  Користувачу дано прямі посилання для самоперевірки (VirusTotal, Google
  Safe Browsing transparency report, Sucuri SiteCheck) і запропоновано
  тест через VPN — **відповіді користувача ще не надійшли**, тема
  залишається відкритою, не активно веденою.

## Справочно — не задача, но легко перепутать

- `mocup/TZ_kontent_32_storinky.md` и `mocup/Google Ads послуги.xlsx` —
  уже отработанные исходники, не новое ТЗ.
- Standalone/агрессивный форк (`koval-group.pp.ua`,
  `github.com/ignis-gg/koval-group-live`) — отдельный проект, отдельный
  репозиторий, не путать с основным `123chush`. Изменения в одном не
  переносятся в другой автоматически.

## Снимки репозитория (справочно, не трогать)

**2026-09-05**: [ignis-gg/123chush-snapshot-2026-09-05](https://github.com/ignis-gg/123chush-snapshot-2026-09-05)
на коммите `5ce945ec6128cc45e69c2a7279d9d3cfb23e1888`.

**2026-09-06**: [ignis-gg/123chush-snapshot-2026-09-06](https://github.com/ignis-gg/123chush-snapshot-2026-09-06)
на коммите `46f617d` — знято ДО другого раунду видалень цієї сесії,
не оновлюється.

Оба — разовые операции, не обновляются автоматически. Работа
продолжается только в `github.com/ignis-gg/123chush`.
