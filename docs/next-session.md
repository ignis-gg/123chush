# Следующая сессия — что делать

Обновлено: 2026-09-06 (сессия 7). Этот файл перезаписывается каждой
сессией под актуальное состояние — не накапливать закрытые пункты.
Полная картина готовности — `docs/migration-status.md`.

## Закрыто в этой сессии (справочно)

- Chrome-расширение переподключено, работает (сессия 6 была без него).
- Демо (koval-legal-demo.pages.dev) пересобрано и передеплоено:
  контраст-фиксы стрелок + `oleg-koval.jpg` fallback теперь реально на
  деплое. Найден и исправлен **новый, пятый** wget-баг (webp `<source
  srcset>` с захардкоженным dev-доменом) — см. known-issues.md. Визуально
  подтверждено в Chrome (главная, `/pro-nas/`, услуга со степпером, блог).
- Email-получатель формы настроен (`company_email` theme_mod →
  `callcenter.via.klg@gmail.com`, тот же адрес, что уже был захардкожен
  как публичный контакт в теме). Реальная отправка через форму
  протестирована end-to-end на dev (Mailpit), тестовый lead удалён.
  **Доставка на реальный Gmail с dev не проверяема** — нужен настоящий
  SMTP на реальном домене (см. блокер ниже).

## Приоритет 1 — визуальный контраст/tab-order (перенесено из сессии 6)

- **Полный аудит контраста WCAG AA по всем типам страниц.** Точечные
  фиксы (`.service-num`/`.process-num`, `.stepper-card`/`.svc-body
  .process-step` стрелки) теперь подтверждены **[browser]** на демо.
  Системный обход по всем типам страниц (главная, услуга, pillar,
  каталог, блог, про нас, контакти, ціни) на dev/demo — ещё не сделан,
  только выборочно то, что попалось в процессе проверки экспорта.
- **Полный обход tab-order клавиатурой** по всем 58+ страницам —
  порядок фокуса, видимость focus-states на каждом интерактивном
  элементе. Code-level часть уже чиста (см. migration-status.md), реальный
  keyboard walk не проводился.

## Приоритет 2

- **Resource hints / defer-async аудит** — `main.js` уже перед `</body>`,
  explicit `preconnect`/`dns-prefetch`/`preload` нигде не расставлены.
- **Кэш-заголовки статики на реальном хостинге** — только когда появится
  доступ к настоящему Apache/LiteSpeed (Этап C).
- **CSP-заголовок** — только с одновременной браузерной проверкой на
  реальном домене (Этап C).
- Решить, нужен ли **cookie-consent баннер** — юридическое решение, не
  техническое.

## Не трогать (заблокировано решением клиента)

См. `docs/migration-status.md` → "Блокеры":
- **Домен (старый/новый)** — ничего из Этапа B (`wp search-replace`,
  DNS/SSL, redirect-карта) не начинать раньше этого решения.
- **Настоящий SMTP на домене** — email-получатель формы уже настроен
  (см. выше), но реальная доставка без риска спама требует
  SPF/DKIM-аутентифицированного SMTP на реальном домене, которого пока
  нет. Не настраивать Gmail App Password/SendGrid наугад без решения
  клиента о домене/почте.

## Снимок репозитория (справочно, не трогать)

**2026-09-05**: независимый снимок-репозиторий
[ignis-gg/123chush-snapshot-2026-09-05](https://github.com/ignis-gg/123chush-snapshot-2026-09-05)
(private) на коммите `5ce945ec6128cc45e69c2a7279d9d3cfb23e1888` — разовая
операция, не обновляется автоматически. Работа продолжается только в
`github.com/ignis-gg/123chush`.

## Справочно — не задача, но легко перепутать

- `mocup/TZ_kontent_32_storinky.md` и `mocup/Google Ads послуги.xlsx` —
  **уже отработанные исходники, не новая работа.** Контент из них уже
  полностью перенесён (32 лендинга → posts 133-164; xlsx — источник уже
  закрытого Google Ads compliance-аудита). Не начинать заново работу по
  этим файлам, посчитав их "новым ТЗ" — эта ошибка уже была допущена
  один раз в сессии 5.
